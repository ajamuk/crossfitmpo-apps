/* support.js — Design Component (.dc.html) runtime, generated by the claude-design skill.
 * Vendored: preact 10.26.4 (+hooks +compat, MIT license) as the React implementation.
 * This file is written for you by scripts/dc.mjs — do not edit by hand. */
!function(n,t){"object"==typeof exports&&"undefined"!=typeof module?t(exports):"function"==typeof define&&define.amd?define(["exports"],t):t((n||self).preact={})}(this,function(n){var t,i,e,r,f,o,u,c,s,a,h,p,l,y="http://www.w3.org/2000/svg",v="http://www.w3.org/1999/xhtml",d=null,w=void 0,g={},_=[],b=/acit|ex(?:s|g|n|p|$)|rph|grid|ows|mnc|ntw|ine[ch]|zoo|^ord|itera/i,m=Array.isArray;

return M(n,u,r,f,d)}function M(n,t,r,f,o){var u={type:n,props:t,key:r,ref:f,__k:d,__:d,__b:0,__e:d,__c:d,constructor:w,__v:o==d?++e:o,__i:-1,__u:0};return o==d&&i.vnode!=d&&i.vnode(u),u}function T(n){return n.children}function $(n,t){this.props=n,this.context=t}function C(n,t){if(t==d)return n.__?C(n.__,n.__i+1):d;for(var i;t<n.__k.length;t++)if((i=n.__k[t])!=d&&i.__e!=d)return i.__e;return"function"==typeof n.type?C(n):d}function I(n){var t,i;if((n=n.__)!=d&&n.__c!=d){for(n.__e=n.__c.base=d,t=0;t<n.__k.length;t++)if((i=n.__k[t])!=d&&i.__e!=d){

var f,o,u,c,s,a=i.length,h=a,p=0;for(n.__k=new Array(r),f=0;f<r;f++)(o=t[f])!=d&&"boolean"!=typeof o&&"function"!=typeof o?(c=f+p,(o=n.__k[f]="string"==typeof o||"number"==typeof o||"bigint"==typeof o||o.constructor==String?M(d,o,d,d,d):m(o)?M(T,{children:o},d,d,d):o.constructor===w&&o.__b>0?M(o.type,o.props,o.key,o.ref?o.ref:d,o.__v):o).__=n,o.__b=n.__b+1,s=o.__i=F(o,i,c,h),u=d,-1!==s&&(h--,(u=i[s])&&(u.__u|=2)),u==d||u.__v===d?(-1==s&&(r>a?p--:r<a&&p++),"function"!=typeof o.type&&(o.__u|=4)):s!=c&&(s==c-1?p--:s==c+1?p++:(s>c?p--:p++,o.__u|=4))):n.__k[f]=d;

var h,p,l,y,v,g,_,b,S,M,C,I,P,j,H,L,F,O=t.type;if(t.constructor!==w)return d;128&e.__u&&(s=!!(32&e.__u),o=[c=t.__e=e.__e]),(h=i.__b)&&h(t);

},p.__s)),k(p.__s,O.getDerivedStateFromProps(b,p.__s))),y=p.props,v=p.state,p.__v=t,l)S&&O.getDerivedStateFromProps==d&&p.componentWillMount!=d&&p.componentWillMount(),S&&p.componentDidMount!=d&&p.__h.push(p.componentDidMount);else{if(S&&O.getDerivedStateFromProps==d&&b!==y&&p.componentWillReceiveProps!=d&&p.componentWillReceiveProps(b,C),!p.__e&&(p.shouldComponentUpdate!=d&&!1===p.shouldComponentUpdate(b,p.__s,C)||t.__v==e.__v)){for(t.__v!=e.__v&&(p.props=b,p.state=p.__s,p.__d=!1),t.__e=e.__e,t.__k=e.__k,t.__k.some(function(n){n&&(n.__=t)}),I=0;I<p._sb.length;I++)p.__h.push(p._sb[I]);p._sb=[],p.__h.length&&u.push(p);break n}p.componentWillUpdate!=d&&p.componentWillUpdate(b,p.__s,C),S&&p.componentDidUpdate!=d&&p.__h.push(function(){

},r),p.getChildContext())),S&&!l&&p.getSnapshotBeforeUpdate!=d&&(g=p.getSnapshotBeforeUpdate(y,v)),L=h,h!=d&&h.type===T&&h.key==d&&(L=B(h.props.children)),c=A(n,m(L)?L:[L],t,e,r,f,o,u,c,s,a),p.base=t.__e,t.__u&=-161,p.__h.length&&u.push(p),_&&(p.__E=p.__=d)}catch(n){if(t.__v=d,s||o!=d)if(n.then){

)}function B(n){return"object"!=typeof n||n==d?n:m(n)?n.map(B):k({},n)}function D(n,e,r,f,o,u,c,s,a){var h,p,l,_,b,k,S,M=r.props,T=e.props,$=e.type;if("svg"==$?o=y:"math"==$?o="http://www.w3.org/1998/Math/MathML":o||(o=v),u!=d)for(h=0;h<u.length;h++)if((b=u[h])&&"setAttribute"in b==!!$&&($?b.localName==$:3==b.nodeType)){n=b,u[h]=d;break}if(n==d){if($==d)return document.createTextNode(T);

&k==d?n.removeAttribute("value"):k!==w&&(k!==n[h]||"progress"==$&&!k||"option"==$&&k!==M[h])&&z(n,h,k,M[h],o),h="checked",S!==w&&S!==n[h]&&z(n,h,S,M[h],o))}return n}function E(n,t,e){try{if("function"==typeof n){var r="function"==typeof n.__u;r&&n.__u(),r&&t==d||(n.__u=n(t))}else n.current=t}catch(n){i.__e(n,e)}}function G(n,t,e){var r,f;if(i.unmount&&i.unmount(n),(r=n.ref)&&(r.current&&r.current!==n.__e||E(r,d,t)),(r=n.__c)!=d){

o=(f="function"==typeof r)?d:r&&r.__k||e.__k,u=[],c=[],V(e,n=(!f&&r||e).__k=S(T,d,[n]),o||g,g,e.namespaceURI,!f&&r?[r]:o?d:e.firstChild?t.call(e.childNodes):d,u,!f&&r?r:o?o.__e:e.firstChild,f,c),q(u,n,c)}t=_.slice,i={__e:function(n,t,i,e){for(var r,f,o;t=t.__;)if((r=t.__c)&&!r.__)try{if((f=r.constructor)&&f.getDerivedStateFromError!=d&&(r.setState(f.getDerivedStateFromError(n)),o=r.__d),r.componentDidCatch!=d&&(r.componentDidCatch(n,e||{}),o=r.__d),o)return r.__E=r}catch(t){n=t}throw n}},e=0,r=function(n){return n!=d&&n.constructor==w},$.prototype.setState=function(n,t){var i;i=this.__s!=d&&this.__s!==this.state?this.__s:this.__s=k({

var r,f,o,u,c=k({},n.props);for(o in n.type&&n.type.defaultProps&&(u=n.type.defaultProps),i)"key"==o?r=i[o]:"ref"==o?f=i[o]:c[o]=i[o]===w&&u!==w?u[o]:i[o];

return{current:d}},n.h=S,n.hydrate=function n(t,i){K(t,i,n)},n.isValidElement=r,n.options=i,n.render=K,n.toChildArray=function n(t,i){return i=i||[],t==d||"boolean"==typeof t||(m(t)?t.some(function(t){n(t,i)}):i.push(t)),i}});
//# sourceMappingURL=preact.umd.js.map

!function(n,t){"object"==typeof exports&&"undefined"!=typeof module?t(exports,require("preact")):"function"==typeof define&&define.amd?define(["exports","preact"],t):t((n||self).preactHooks={},n.preact)}(this,function(n,t){var u,i,r,o,f=0,c=[],e=t.options,a=e.__b,v=e.__r,l=e.diffed,d=e.__c,s=e.unmount,p=e.__;

return f=8,_(function(){return n},t)},n.useContext=function(n){var t=i.context[n.__c],r=y(u++,9);return r.c=n,t?(null==r.__&&(r.__=!0,t.sub(i)),t.props.value):n.__},n.useDebugValue=function(n,t){e.useDebugValue&&e.useDebugValue(t?t(n):n)},n.useEffect=function(n,t){var r=y(u++,3);!e.__s&&g(r.__H,t)&&(r.__=n,r.u=t,i.__H.__h.push(r))},n.useErrorBoundary=function(n){var t=y(u++,10),r=h();return t.__=n,i.componentDidCatch||(i.componentDidCatch=function(n,u){t.__&&t.__(n,u),r[1](n)}),[r[0],function(){r[1](void 0)}]},n.useId=function(){var n=y(u++,11);if(!n.__){for(var t=i.__v;null!==t&&!t.__m&&null!==t.__;)t=t.__;var r=t.__m||(t.__m=[0,0]);n.__="P"+r[0]+"-"+r[1]++}return n.__},n.useImperativeHandle=function(n,t,u){f=6,T(function(){if("function"==typeof n){var u=n(t());return function(){n(null),u&&"function"==typeof u&&u()}}if(n)return n.current=t(),function(){return n.current=null}},null==u?u:u.concat(n))},n.useLayoutEffect=T,n.useMemo=_,n.useReducer=m,n.useRef=function(n){return f=5,_(function(){return{current:n}},[])},n.useState=h});
//# sourceMappingURL=hooks.umd.js.map

!function(n,t){"object"==typeof exports&&"undefined"!=typeof module?t(exports,require("preact"),require("preact/hooks")):"function"==typeof define&&define.amd?define(["exports","preact","preact/hooks"],t):t((n||self).preactCompat={},n.preact,n.preactHooks)}(this,function(n,t,e){function r(n,t){for(var e in t)n[e]=t[e];return n}function u(n,t){for(var e in n)if("__source"!==e&&!(e in t))return!0;for(var r in t)if("__source"!==r&&n[r]!==t[r])return!0;return!1}function o(n,t){var r=t(),u=e.useState({t:{__:r,u:t}}),o=u[0].t,c=u[1];return e.useLayoutEffect(function(){o.__=r,o.u=t,i(o)&&c({t:o})},[n,r,t]),e.useEffect(function(){return i(o)&&c({t:o}),n(function(){i(o)&&c({t:o})})},[n]),r}function i(n){var t,e,r=n.u,u=n.__;try{var o=r();return!((t=u)===(e=o)&&(0!==t||1/t==1/e)||t!=t&&e!=e)}catch(n){return!0}}function c(n){n()}function f(n){return n}function l(){return[!1,c]}var a=e.useLayoutEffect;function s(n,t){

,b={map:m,forEach:m,count:function(n){return n?t.toChildArray(n).length:0},only:function(n){var e=t.toChildArray(n);

.useCallback,useContext:e.useContext,useDebugValue:e.useDebugValue,useDeferredValue:f,useEffect:e.useEffect,useId:e.useId,useImperativeHandle:e.useImperativeHandle,useInsertionEffect:a,useLayoutEffect:e.useLayoutEffect,useMemo:e.useMemo,useReducer:e.useReducer,useRef:e.useRef,useState:e.useState,useSyncExternalStore:o,useTransition:l}}},$="18.3.1";

useState:e.useState,useId:e.useId,useReducer:e.useReducer,useEffect:e.useEffect,useLayoutEffect:e.useLayoutEffect,useInsertionEffect:a,useTransition:l,useDeferredValue:f,useSyncExternalStore:o,startTransition:c,

enumerable:!0,get:function(){return t.createContext}}),Object.defineProperty(n,"createElement",{enumerable:!0,get:function(){return t.createElement}}),Object.defineProperty(n,"createRef",{enumerable:!0,get:function(){return t.createRef}}),n.Children=b,n.PureComponent=s,n.StrictMode=un,n.Suspense=E,n.SuspenseList=O,n.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=Y,n.cloneElement=X,n.createFactory=G,n.createPortal=T,n.default=cn,n.findDOMNode=tn,n.flushSync=rn,n.forwardRef=p,n.hydrate=F,n.isElement=on,n.isFragment=K,n.isMemo=Q,n.isValidElement=J,n.lazy=x,n.memo=h,n.render=L,n.startTransition=c,n.unmountComponentAtNode=nn,n.unstable_batchedUpdates=en,n.useDeferredValue=f,n.useInsertionEffect=a,n.useSyncExternalStore=o,n.useTransition=l,n.version=$,Object.keys(e).forEach(function(t){"default"===t||n.hasOwnProperty(t)||Object.defineProperty(n,t,{enumerable:!0,get:function(){return e[t]}})})});
//# sourceMappingURL=compat.umd.js.map

/* ═══ Design Component runtime ═══
 * Implements the .dc.html format: <x-dc> templates with {{ path }} holes,
 * <sc-for>/<sc-if> control flow, <dc-import>/<x-import> mounts, <helmet>
 * head hoisting, style-hover/active/focus/before/after pseudo styles, and
 * the DCLogic component class (React-like, backed by Preact).
 * Diagnostics are exposed in a <script id="__dc_diag"> JSON node for the
 * verify script, plus an on-page error overlay.
 */
(function () {
  'use strict';
  var preact = window.preact;
  var compat = window.preactCompat;
  var h = preact.h;

  if (!window.React) { window.React = compat; }
  if (!window.ReactDOM) { window.ReactDOM = compat; }

  /* ---------- diagnostics ---------- */
  var diag = window.__dcDiag || { errors: [], warnings: [], ready: false };
  window.__dcDiag = diag;
  var diagEl = null;
  function syncDiag() {
    try {
      if (!diagEl) diagEl = document.getElementById('__dc_diag');
      if (!diagEl) {
        diagEl = document.createElement('script');
        diagEl.type = 'application/json';
        diagEl.id = '__dc_diag';
        document.documentElement.appendChild(diagEl);
      }
      diagEl.textContent = JSON.stringify({
        errors: diag.errors.slice(0, 50),
        warnings: diag.warnings.slice(0, 50),
        ready: !!diag.ready
      });
    } catch (e) { /* diagnostics must never break the page */ }
  }
  function fmtArg(a) {
    if (typeof a === 'string') return a;
    if (a && a.stack) return String(a.stack).split('\n').slice(0, 3).join(' | ');
    try { return JSON.stringify(a); } catch (e) { return String(a); }
  }
  function report(kind, msg) {
    msg = String(msg).slice(0, 2000);
    diag[kind].push(msg);
    syncDiag();
    if (kind === 'errors') showOverlay();
  }
  if (!window.__dcDiagListeners) {
    window.__dcDiagListeners = true;
    window.addEventListener('error', function (e) {
      report('errors', (e.message || 'Script error') + (e.filename ? ' @ ' + e.filename + ':' + e.lineno : ''));
    });
    window.addEventListener('unhandledrejection', function (e) {
      var r = e.reason;
      report('errors', 'Unhandled rejection: ' + ((r && (r.stack || r.message)) || r));
    });
    var _cerr = console.error.bind(console);
    console.error = function () { report('errors', Array.prototype.map.call(arguments, fmtArg).join(' ')); _cerr.apply(null, arguments); };
    var _cwarn = console.warn.bind(console);
    console.warn = function () { report('warnings', Array.prototype.map.call(arguments, fmtArg).join(' ')); _cwarn.apply(null, arguments); };
  }

  var overlayEl = null;
  function showOverlay() {
    try {
      if (!document.body) return;
      if (!overlayEl) {
        overlayEl = document.createElement('div');
        overlayEl.id = '__dc_error_overlay';
        overlayEl.style.cssText = 'position:fixed;left:12px;right:12px;bottom:12px;z-index:2147483647;background:#3b0d0d;color:#ffd7d7;font:12px/1.5 ui-monospace,monospace;padding:10px 36px 10px 14px;border-radius:10px;box-shadow:0 8px 30px rgba(0,0,0,.4);max-height:38vh;overflow:auto;white-space:pre-wrap;';
        var x = document.createElement('button');
        x.textContent = '×';
        x.style.cssText = 'position:absolute;top:6px;right:8px;background:none;border:none;color:#ffd7d7;font-size:16px;cursor:pointer;';
        x.onclick = function () { overlayEl.remove(); overlayEl = null; };
        overlayEl.appendChild(x);
        var pre = document.createElement('div');
        pre.className = '__dc_error_list';
        overlayEl.appendChild(pre);
        document.body.appendChild(overlayEl);
      }
      overlayEl.querySelector('.__dc_error_list').textContent = diag.errors.slice(-8).join('\n──\n');
    } catch (e) { /* ignore */ }
  }

  /* ---------- hole resolution ---------- */
  var HOLE_RE = /\{\{([\s\S]*?)\}\}/g;
  var warnedHoles = {};
  function resolvePath(expr, scope) {
    expr = expr.trim();
    if (expr === '') return undefined;
    if (expr === 'true') return true;
    if (expr === 'false') return false;
    if (expr === 'null') return null;
    if (expr === 'undefined') return undefined;
    if (/^-?\d+(\.\d+)?$/.test(expr)) return parseFloat(expr);
    var q = expr.match(/^(['"])([\s\S]*)\1$/);
    if (q) return q[2];
    if (!/^[$A-Za-z_][$\w]*(\s*\.\s*[$\w]+)*$/.test(expr)) {
      if (!warnedHoles[expr]) {
        warnedHoles[expr] = true;
        console.warn('[dc] Hole {{ ' + expr + ' }} is not a dotted path — compute it in renderVals() and expose it by name.');
      }
      return undefined;
    }
    var parts = expr.split('.');
    var cur = scope;
    for (var i = 0; i < parts.length; i++) {
      if (cur == null) return undefined;
      cur = cur[parts[i].trim()];
    }
    return cur;
  }
  function hasHole(s) { return s.indexOf('{{') !== -1; }
  function wholeHole(s) {
    var m = s.match(/^\s*\{\{([\s\S]*?)\}\}\s*$/);
    return m ? m[1] : null;
  }
  function interp(s, scope) {
    return s.replace(HOLE_RE, function (_, e) {
      var v = resolvePath(e, scope);
      return v === undefined || v === null ? '' : String(v);
    });
  }

  /* ---------- style parsing ---------- */
  function splitDecls(str) {
    var parts = [], cur = '', depth = 0;
    for (var i = 0; i < str.length; i++) {
      var c = str[i];
      if (c === '(') depth++;
      else if (c === ')') depth = Math.max(0, depth - 1);
      if (c === ';' && depth === 0) { parts.push(cur); cur = ''; }
      else cur += c;
    }
    if (cur.trim()) parts.push(cur);
    return parts;
  }
  function parseStyle(str) {
    var out = {};
    splitDecls(str).forEach(function (decl) {
      var idx = decl.indexOf(':');
      if (idx < 0) return;
      var prop = decl.slice(0, idx).trim();
      var val = decl.slice(idx + 1).trim();
      if (!prop) return;
      if (prop.indexOf('--') !== 0) prop = prop.replace(/-([a-z])/g, function (_, ch) { return ch.toUpperCase(); });
      out[prop] = val;
    });
    return out;
  }
  function parseHintSize(s) {
    if (!s) return { w: '100%', h: '80px' };
    var parts = String(s).split(',');
    return { w: (parts[0] || '100%').trim(), h: (parts[1] || '80px').trim() };
  }

  /* ---------- ::before / ::after rules ---------- */
  var pseudoNodes = {};
  var pseudoUid = 0;
  function setPseudoRule(uid, which, styleStr) {
    var key = uid + '::' + which;
    var node = pseudoNodes[key];
    if (!node) {
      node = document.createElement('style');
      node.setAttribute('data-dc-pseudo', key);
      document.head.appendChild(node);
      pseudoNodes[key] = node;
    }
    if (!/(^|;)\s*content\s*:/.test(styleStr)) styleStr = 'content:"";' + styleStr;
    var css = '[data-dcp="' + uid + '"]::' + which + '{' + styleStr + '}';
    if (node.textContent !== css) node.textContent = css;
  }

  /* ---------- placeholder ---------- */
  function placeholderBox(hintSize) {
    var size = parseHintSize(hintSize);
    return h('div', {
      'data-dc-placeholder': '1',
      style: { width: size.w, height: size.h, background: 'rgba(127,127,127,0.12)', borderRadius: '8px' }
    });
  }

  /* ---------- pseudo-state element ---------- */
  function chainHandler(existing, extra) {
    if (!existing) return extra;
    return function (ev) { extra(ev); return existing.call(this, ev); };
  }
  var StatefulEl = /** @type {any} */ (function () {
    function S() { preact.Component.apply(this, arguments); this.state = { hover: false, active: false, focus: false }; }
    S.prototype = Object.create(preact.Component.prototype);
    S.prototype.constructor = S;
    S.prototype.render = function () {
      var self = this;
      var p = Object.assign({}, this.props.eprops);
      var styles = this.props.dcStyles;
      var st = Object.assign({}, styles.base);
      if (this.state.hover && styles.hover) Object.assign(st, styles.hover);
      if (this.state.active && styles.active) Object.assign(st, styles.active);
      if (this.state.focus && styles.focus) Object.assign(st, styles.focus);
      p.style = st;
      if (styles.hover) {
        p.onmouseenter = chainHandler(p.onmouseenter, function () { self.setState({ hover: true }); });
        p.onmouseleave = chainHandler(p.onmouseleave, function () { self.setState({ hover: false, active: false }); });
      }
      if (styles.active) {
        p.onmousedown = chainHandler(p.onmousedown, function () { self.setState({ active: true }); });
        p.onmouseup = chainHandler(p.onmouseup, function () { self.setState({ active: false }); });
        p.onmouseleave = chainHandler(p.onmouseleave, function () { self.setState({ active: false }); });
      }
      if (styles.focus) {
        p.onfocusin = chainHandler(p.onfocusin, function () { self.setState({ focus: true }); });
        p.onfocusout = chainHandler(p.onfocusout, function () { self.setState({ focus: false }); });
      }
      return h(this.props.dcTag, p, this.props.children);
    };
    return S;
  })();

  /* ---------- helmet hoisting ---------- */
  var helmetSeen = {};
  function hoistHelmet(el) {
    Array.prototype.slice.call(el.childNodes).forEach(function (n) {
      if (n.nodeType !== 1) return;
      var t = n.tagName.toLowerCase();
      var key = t === 'script' ? n.getAttribute('src') : t === 'link' ? n.getAttribute('href') : null;
      if (key) {
        if (helmetSeen[t + ' ' + key]) return;
        helmetSeen[t + ' ' + key] = true;
      }
      var clone;
      if (t === 'script') {
        clone = document.createElement('script');
        Array.prototype.forEach.call(n.attributes, function (a) { clone.setAttribute(a.name, a.value); });
        clone.textContent = n.textContent;
        // Dynamically-inserted scripts default to async — force in-order
        // execution so helmet script chains (gsap -> ScrollTrigger) work.
        clone.async = false;
      } else {
        clone = n.cloneNode(true);
      }
      document.head.appendChild(clone);
    });
  }

  /* ---------- module loading (x-import) ---------- */
  var moduleCache = {};
  function fetchHint(url, err) {
    var msg = 'Failed to load "' + url + '": ' + (err && err.message || err);
    if (location.protocol === 'file:') {
      msg += ' — browsers block imports from file://. Serve this folder over HTTP (e.g. `python3 -m http.server`) and open it via http://localhost.';
    }
    return new Error(msg);
  }
  function loadBabel() {
    if (window.Babel) return Promise.resolve(window.Babel);
    if (!window.__dcBabelPromise) {
      window.__dcBabelPromise = new Promise(function (res, rej) {
        var s = document.createElement('script');
        s.src = 'https://unpkg.com/@babel/standalone@7/babel.min.js';
        s.onload = function () { res(window.Babel); };
        s.onerror = function () { rej(new Error('Could not load Babel from unpkg.com (needed to transpile a .jsx import; requires network access).')); };
        document.head.appendChild(s);
      });
    }
    return window.__dcBabelPromise;
  }
  function loadModule(url) {
    if (moduleCache[url]) return moduleCache[url];
    moduleCache[url] = fetch(url)
      .then(function (r) {
        if (!r.ok) throw new Error('HTTP ' + r.status);
        return r.text();
      })
      .catch(function (err) { throw fetchHint(url, err); })
      .then(function (code) {
        var isJsx = /\.jsx([?#]|$)/.test(url);
        var jsP = isJsx
          ? loadBabel().then(function (Babel) {
              return Babel.transform(code, { presets: ['react'], filename: url }).code;
            })
          : Promise.resolve(code);
        return jsP.then(function (js) {
          var module = { exports: {} };
          var fn = new Function('module', 'exports', 'React', 'ReactDOM', js + '\n//# sourceURL=' + url);
          fn(module, module.exports, window.React, window.ReactDOM);
          return module.exports;
        });
      });
    return moduleCache[url];
  }
  function lookupGlobal(path) {
    var cur = window;
    var parts = path.split('.');
    for (var i = 0; i < parts.length; i++) {
      if (cur == null) return undefined;
      cur = cur[parts[i]];
    }
    return cur;
  }
  function waitForGlobal(path, timeoutMs) {
    var existing = lookupGlobal(path);
    if (existing !== undefined) return Promise.resolve(existing);
    return new Promise(function (res, rej) {
      var t0 = Date.now();
      var iv = setInterval(function () {
        var v = lookupGlobal(path);
        if (v !== undefined) { clearInterval(iv); res(v); }
        else if (Date.now() - t0 > (timeoutMs || 10000)) {
          clearInterval(iv);
          rej(new Error('Global "' + path + '" never appeared (waited ' + (timeoutMs || 10000) + 'ms).'));
        }
      }, 50);
    });
  }

  function errorBox(msg) {
    return h('div', {
      style: {
        padding: '12px', background: '#3b0d0d', color: '#ffd7d7',
        font: '12px/1.5 ui-monospace,monospace', borderRadius: '8px', whiteSpace: 'pre-wrap'
      }
    }, String(msg));
  }

  /* ---------- <x-import> ---------- */
  var XImport = (function () {
    function X() { preact.Component.apply(this, arguments); this.state = { comp: null, tag: null, err: null }; }
    X.prototype = Object.create(preact.Component.prototype);
    X.prototype.constructor = X;
    X.prototype.componentDidMount = function () {
      var self = this;
      var p = this.props;
      var fromGlobal = p.dcGlobalName;
      var name = p.dcComponentName || fromGlobal;
      var loaded = p.dcFrom ? loadModule(p.dcFrom) : Promise.resolve(null);
      loaded
        .then(function (exports) {
          if (!name) throw new Error('<x-import> needs component="..." or component-from-global-scope="..."');
          if (fromGlobal) {
            if (name.indexOf('-') !== -1) {
              // custom element tag
              var race = new Promise(function (_, rej) { setTimeout(function () { rej(new Error('custom element <' + name + '> was never defined')); }, 10000); });
              return Promise.race([customElements.whenDefined(name), race]).then(function () {
                self.setState({ tag: name });
              });
            }
            return waitForGlobal(name).then(function (C) { self.setState({ comp: C }); });
          }
          if (exports && exports[name]) { self.setState({ comp: exports[name] }); return; }
          return waitForGlobal(name).then(function (C) { self.setState({ comp: C }); });
        })
        .catch(function (err) {
          report('errors', '<x-import ' + (name || '?') + '>: ' + (err.message || err));
          self.setState({ err: err.message || String(err) });
        });
    };
    X.prototype.render = function () {
      var p = this.props;
      if (this.state.err) return errorBox('x-import failed: ' + this.state.err);
      if (this.state.tag) return h(this.state.tag, p.dcElProps, p.children);
      if (this.state.comp) return h(this.state.comp, p.dcElProps, p.children);
      return placeholderBox(p.dcHintSize);
    };
    return X;
  })();

  /* ---------- <dc-import> ---------- */
  var dcFileCache = {};
  function loadDC(name) {
    var url = name + '.dc.html';
    if (dcFileCache[url]) return dcFileCache[url];
    dcFileCache[url] = fetch(url)
      .then(function (r) {
        if (!r.ok) throw new Error('HTTP ' + r.status);
        return r.text();
      })
      .catch(function (err) { throw fetchHint(url, err); })
      .then(function (text) {
        var doc = new DOMParser().parseFromString(text, 'text/html');
        var x = doc.querySelector('x-dc');
        if (!x) throw new Error(url + ' has no <x-dc> template');
        Array.prototype.slice.call(x.querySelectorAll('helmet')).forEach(function (hm) {
          hoistHelmet(hm);
          hm.remove();
        });
        var tpl = compileChildren(x.childNodes);
        var ls = doc.querySelector('script[data-dc-script]');
        var Component = makeComponent(ls ? ls.textContent : '', url);
        return { tpl: tpl, Component: Component };
      });
    return dcFileCache[url];
  }
  var DCImport = (function () {
    function D() { preact.Component.apply(this, arguments); this.state = { mod: null, err: null }; }
    D.prototype = Object.create(preact.Component.prototype);
    D.prototype.constructor = D;
    D.prototype.load = function () {
      var self = this;
      var name = this.props.dcName;
      loadDC(name)
        .then(function (mod) { self.setState({ mod: mod, err: null }); })
        .catch(function (err) {
          report('errors', '<dc-import name="' + name + '">: ' + (err.message || err));
          self.setState({ err: err.message || String(err) });
        });
    };
    D.prototype.componentDidMount = function () { this.load(); };
    D.prototype.componentDidUpdate = function (prev) { if (prev.dcName !== this.props.dcName) { this.setState({ mod: null }); this.load(); } };
    D.prototype.render = function () {
      var p = this.props;
      if (this.state.err) return errorBox('dc-import failed: ' + this.state.err);
      var mountStyle = p.dcMountStyle;
      var inner;
      if (!this.state.mod) {
        inner = placeholderBox(p.dcHintSize);
      } else {
        var childProps = Object.assign({ __dcTemplate: this.state.mod.tpl }, p.dcElProps);
        inner = h(this.state.mod.Component, childProps);
      }
      var hint = parseHintSize(p.dcHintSize);
      var wrapStyle = Object.assign({ minWidth: hint.w === '100%' ? undefined : hint.w, minHeight: hint.h }, mountStyle);
      return h('div', { style: wrapStyle, 'data-dc-import': p.dcName }, inner);
    };
    return D;
  })();

  /* ---------- template compiler ---------- */
  function kebabToCamel(s) { return s.replace(/-([a-z])/g, function (_, c) { return c.toUpperCase(); }); }

  function attrProducer(val) {
    var wh = wholeHole(val);
    if (wh !== null) return function (scope) { return resolvePath(wh, scope); };
    if (hasHole(val)) return function (scope) { return interp(val, scope); };
    return null; // static
  }

  function compileChildren(nodeList) {
    var fns = [];
    Array.prototype.slice.call(nodeList).forEach(function (n) {
      var f = compileNode(n);
      if (f) fns.push(f);
    });
    return function (scope) {
      return fns.map(function (f) { return f(scope); });
    };
  }

  function compileNode(node) {
    if (node.nodeType === 3) { // text
      var text = node.data;
      if (!text.trim() && text.indexOf('{{') === -1) return null;
      if (!hasHole(text)) return function () { return text; };
      return function (scope) { return interp(text, scope); };
    }
    if (node.nodeType !== 1) return null; // comments etc.
    var tag = node.tagName.toLowerCase();

    if (tag === 'helmet') { hoistHelmet(node); return null; }
    if (tag === 'script') {
      report('warnings', '<script> found inside the template — scripts are only legal inside <helmet>; it will not run.');
      return null;
    }

    if (tag === 'sc-for') {
      var listExpr = wholeHole(node.getAttribute('list') || '') || (node.getAttribute('list') || '');
      var asName = node.getAttribute('as') || 'item';
      var phCount = parseInt(node.getAttribute('hint-placeholder-count') || '3', 10) || 3;
      var kids = compileChildren(node.childNodes);
      return function (scope) {
        var list = resolvePath(listExpr, scope);
        if (list === undefined) {
          var phs = [];
          for (var i = 0; i < phCount; i++) {
            var s = Object.assign({}, scope);
            s[asName] = undefined;
            s.$index = i;
            phs.push(h(preact.Fragment, { key: 'ph' + i }, kids(s)));
          }
          return phs;
        }
        if (!Array.isArray(list)) {
          console.warn('[dc] <sc-for list="{{ ' + listExpr + ' }}"> resolved to a non-array value.');
          return null;
        }
        return list.map(function (item, i) {
          var s = Object.assign({}, scope);
          s[asName] = item;
          s.$index = i;
          return h(preact.Fragment, { key: i }, kids(s));
        });
      };
    }

    if (tag === 'sc-if') {
      var valExpr = wholeHole(node.getAttribute('value') || '') || (node.getAttribute('value') || '');
      var hintRaw = node.getAttribute('hint-placeholder-val');
      var hintExpr = hintRaw != null ? (wholeHole(hintRaw) || hintRaw) : null;
      var ifKids = compileChildren(node.childNodes);
      return function (scope) {
        var v = resolvePath(valExpr, scope);
        if (v === undefined && hintExpr != null) v = resolvePath(hintExpr, scope);
        return v ? h(preact.Fragment, null, ifKids(scope)) : null;
      };
    }

    if (tag === 'dc-import') {
      var nameProd = attrProducer(node.getAttribute('name') || '');
      var nameStatic = node.getAttribute('name') || '';
      var dcHint = node.getAttribute('hint-size');
      var dcStyleAttr = node.getAttribute('style');
      var propAttrs = [];
      Array.prototype.forEach.call(node.attributes, function (a) {
        if (a.name === 'name' || a.name === 'style' || a.name.indexOf('hint-') === 0) return;
        propAttrs.push({ prop: kebabToCamel(a.name), producer: attrProducer(a.value), value: a.value });
      });
      return function (scope) {
        var elProps = {};
        propAttrs.forEach(function (pa) { elProps[pa.prop] = pa.producer ? pa.producer(scope) : pa.value; });
        return h(DCImport, {
          dcName: nameProd ? nameProd(scope) : nameStatic,
          dcHintSize: dcHint,
          dcMountStyle: dcStyleAttr ? parseStyle(hasHole(dcStyleAttr) ? interp(dcStyleAttr, scope) : dcStyleAttr) : null,
          dcElProps: elProps
        });
      };
    }

    if (tag === 'x-import') {
      var compName = node.getAttribute('component');
      var globalName = node.getAttribute('component-from-global-scope');
      var from = node.getAttribute('from'); // must be a literal URL
      var xHint = node.getAttribute('hint-size');
      var xStyleAttr = node.getAttribute('style');
      var isCustomEl = !!(globalName && globalName.indexOf('-') !== -1);
      var xAttrs = [];
      Array.prototype.forEach.call(node.attributes, function (a) {
        if (['component', 'component-from-global-scope', 'from', 'style'].indexOf(a.name) !== -1) return;
        if (a.name.indexOf('hint-') === 0) return;
        var prop = a.name;
        if (a.name === 'dc-props') prop = '__dcSpread';
        else if (!isCustomEl && !/^(data-|aria-)/.test(a.name)) prop = kebabToCamel(a.name);
        xAttrs.push({ prop: prop, producer: attrProducer(a.value), value: a.value });
      });
      var xKids = compileChildren(node.childNodes);
      return function (scope) {
        var elProps = {};
        xAttrs.forEach(function (pa) {
          var v = pa.producer ? pa.producer(scope) : pa.value;
          if (pa.prop === '__dcSpread') { if (v && typeof v === 'object') Object.assign(elProps, v); }
          else elProps[pa.prop] = v;
        });
        if (xStyleAttr) elProps.style = parseStyle(hasHole(xStyleAttr) ? interp(xStyleAttr, scope) : xStyleAttr);
        var nameP = attrProducer(compName || '');
        var gNameP = attrProducer(globalName || '');
        return h(XImport, {
          dcComponentName: compName ? (nameP ? nameP(scope) : compName) : null,
          dcGlobalName: globalName ? (gNameP ? gNameP(scope) : globalName) : null,
          dcFrom: from,
          dcHintSize: xHint,
          dcElProps: elProps
        }, xKids(scope));
      };
    }

    /* plain element */
    var attrs = []; // {prop, producer|null, value}
    var styles = { base: null, hover: null, active: null, focus: null };
    var styleProducers = {}; // key -> producer for dynamic style strings
    var pseudos = []; // {which, value, producer, uid}
    var elUid = null;

    Array.prototype.forEach.call(node.attributes, function (a) {
      var name = a.name, val = a.value;
      if (name === 'style') {
        var p = hasHole(val) ? function (scope) { return parseStyle(interp(val, scope)); } : null;
        if (p) styleProducers.base = p; else styles.base = parseStyle(val);
        return;
      }
      var mState = name.match(/^style-(hover|active|focus)$/);
      if (mState) {
        var key = mState[1];
        if (hasHole(val)) styleProducers[key] = function (scope) { return parseStyle(interp(val, scope)); };
        else styles[key] = parseStyle(val);
        return;
      }
      var mPseudo = name.match(/^style-(before|after)$/);
      if (mPseudo) {
        if (elUid == null) elUid = ++pseudoUid;
        var which = mPseudo[1];
        if (hasHole(val)) pseudos.push({ which: which, producer: function (scope) { return interp(val, scope); } });
        else setPseudoRule(elUid, which, val);
        return;
      }
      if (name.indexOf('hint-') === 0) return;
      var prop = name;
      if (name === 'for') prop = 'htmlFor';
      var producer = attrProducer(val);
      // A bare boolean attribute (checked, disabled, autoplay, …) parses as "" — pass true so the DOM property is set.
      attrs.push({ prop: prop, producer: producer, value: producer ? undefined : (val === '' ? true : val) });
    });
    if (elUid != null) attrs.push({ prop: 'data-dcp', producer: null, value: String(elUid) });

    var kidsFn = node.childNodes.length ? compileChildren(node.childNodes) : null;
    var hasPseudoState = function (resolvedStyles) {
      return !!(resolvedStyles.hover || resolvedStyles.active || resolvedStyles.focus);
    };

    return function (scope) {
      var props = {};
      attrs.forEach(function (pa) {
        props[pa.prop] = pa.producer ? pa.producer(scope) : pa.value;
      });
      var resolved = {
        base: styleProducers.base ? styleProducers.base(scope) : styles.base,
        hover: styleProducers.hover ? styleProducers.hover(scope) : styles.hover,
        active: styleProducers.active ? styleProducers.active(scope) : styles.active,
        focus: styleProducers.focus ? styleProducers.focus(scope) : styles.focus
      };
      pseudos.forEach(function (ps) { setPseudoRule(elUid, ps.which, ps.producer(scope)); });
      var children = kidsFn ? kidsFn(scope) : null;
      if (hasPseudoState(resolved)) {
        return h(StatefulEl, { dcTag: tag, eprops: props, dcStyles: resolved }, children);
      }
      if (resolved.base) props.style = resolved.base;
      return h(tag, props, children);
    };
  }

  /* ---------- logic class ---------- */
  function DCLogicBase() { preact.Component.apply(this, arguments); }
  DCLogicBase.prototype = Object.create(preact.Component.prototype);
  DCLogicBase.prototype.constructor = DCLogicBase;
  DCLogicBase.prototype.render = function () {
    var vals = {};
    if (typeof this.renderVals === 'function') {
      try { vals = this.renderVals() || {}; }
      catch (e) { report('errors', 'renderVals() threw: ' + (e.stack || e)); }
    }
    var scope = Object.assign({}, this.props, vals);
    delete scope.__dcTemplate;
    var tpl = this.props.__dcTemplate;
    return h(preact.Fragment, null, tpl ? tpl(scope) : null);
  };
  window.DCLogic = DCLogicBase;

  function makeComponent(src, label) {
    if (!src || !src.trim()) return DCLogicBase;
    try {
      var C = new Function('DCLogic', 'React', '"use strict";\n' + src + '\nreturn Component;\n//# sourceURL=' + (label || 'dc-logic.js'))(DCLogicBase, window.React);
      if (typeof C !== 'function') throw new Error('logic source did not define `class Component`');
      return C;
    } catch (e) {
      report('errors', 'Logic class failed to compile' + (label ? ' (' + label + ')' : '') + ': ' + (e.stack || e));
      return function () { return errorBox('Logic error: ' + (e.message || e)); };
    }
  }

  /* ---------- boot ---------- */
  function boot() {
    var base = document.createElement('style');
    base.textContent = 'x-dc{display:block;}';
    document.head.appendChild(base);

    var x = document.querySelector('x-dc');
    if (!x) { diag.ready = true; syncDiag(); return; }

    Array.prototype.slice.call(x.querySelectorAll('helmet')).forEach(function (hm) {
      hoistHelmet(hm);
      hm.remove();
    });

    var ls = document.querySelector('script[data-dc-script]');
    var logicSrc = ls ? ls.textContent : '';
    var meta = null;
    var propsJson = ls && ls.getAttribute('data-props');
    if (propsJson) {
      try { meta = JSON.parse(propsJson); }
      catch (e) { report('errors', 'data-props is not valid JSON: ' + e.message); }
    }

    var tpl;
    try { tpl = compileChildren(x.childNodes); }
    catch (e) {
      report('errors', 'Template failed to compile: ' + (e.stack || e));
      diag.ready = true; syncDiag();
      return;
    }
    x.innerHTML = '';

    var preview = meta && meta.$preview;
    if (preview) {
      if (preview.width != null) x.style.width = typeof preview.width === 'number' ? preview.width + 'px' : String(preview.width);
      if (preview.height != null) x.style.height = typeof preview.height === 'number' ? preview.height + 'px' : String(preview.height);
      x.style.margin = '24px auto';
    }

    var Component = makeComponent(logicSrc, location.pathname.split('/').pop());
    try {
      preact.render(h(Component, { __dcTemplate: tpl }), x);
      x.setAttribute('data-dc-ready', '1');
    } catch (e) {
      report('errors', 'Mount failed: ' + (e.stack || e));
    }
    diag.ready = true;
    syncDiag();
  }

  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot);
  else boot();
})();
